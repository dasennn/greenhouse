"""Services package.

This file is intentionally empty. It marks the directory as a Python package
so other modules can import from `services`. Place reusable, non-UI logic here
(e.g., geometry utilities, models, data access). Keep UI code out of here.
"""
